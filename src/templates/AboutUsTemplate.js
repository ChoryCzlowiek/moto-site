import React from "react";
import { pages } from "../const/pages";

import PageTemplate from "./PageTemplate";

const AboutUsTemplate = () => (
  <PageTemplate location={pages.aboutUs.location}></PageTemplate>
);

export default AboutUsTemplate;
