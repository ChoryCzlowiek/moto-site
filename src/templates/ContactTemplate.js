import React from "react";
import { pages } from "../const/pages";
import PageTemplate from "./PageTemplate";

const ContactTemplate = () => (
  <PageTemplate location={pages.contact.location}></PageTemplate>
);

export default ContactTemplate;
